# OpenRhiza Development Harness
# LLM 교차 검증 기반 개발을 위한 마스터 가이드

> **이 문서의 목적:** 어떤 AI 모델(Gemini, Claude, GPT, Llama 등)이든 이 프로젝트에 참여할 때
> 이 파일을 읽으면 프로젝트의 본질을 정확히 이해하고, 올바르게 코드를 작성/검증할 수 있도록 한다.

---

## 0. 프로젝트 정체성 (Identity)

OpenRhiza는 **AI가 OS 자체가 되는 유기적 시스템**입니다.

- 베어메탈 x86_64 머신에서 부팅되는 `no_std` Rust 커널
- AI가 하드웨어 드라이버 코드를 **WebAssembly로 생성** → **격리된 샌드박스에서 실행** → **결과를 학습**하는 피드백 루프
- **OpenRhiza.com 생태계:** 전 세계 AI 인스턴스들이 자신의 경험(드라이버, 최적화 코드, 문제 해결법)을 공유하고, 각 사용자의 하드웨어/요구에 맞는 최적의 조합을 선택하여 **성장하는 OS**
- **성능이 핵심:** Rust 기반으로 기존 OS 대비 성능 열위를 허용하지 않음. 필요시 C나 어셈블리도 고려.

### 핵심 원칙
1. **키보드 + 모니터 + LAN = OS 자체적 구동 필수.** 이 3가지는 외부 도움 없이 OS 스스로 동작해야 함.
2. **로컬 LLM은 선택적.** 고성능 GPU/NPU/LPU가 있을 때만 탑재. 외부 LLM API는 항상 활용 가능.
3. **샌드박스는 필수.** LLM이 제안한 코드/드라이버에는 오류가 있을 수 있으므로, 핵심 시스템을 제외한 모든 AI 생성 코드는 Wasm 샌드박스에서 실행.
4. **OpenRhiza.com이 뇌의 연장선.** 개별 OS가 Trial & Error로 얻은 경험을 공유하고, 검증된 드라이버/코드를 신뢰도 기반으로 교환.

---

## 1. 필수 선행 문서

| 문서 | 내용 | 언제 읽나 |
|------|------|-----------|
| [ARCHITECTURE.md](ARCHITECTURE.md) | 5계층 아키텍처 & 5단계 진화 시나리오 | 항상 |
| [LEVEL1_BLUEPRINT.md](LEVEL1_BLUEPRINT.md) | Layer 0/1 구현 지침, 샌드박스 철학, USB/디스플레이 전략 | Layer 0~1 작업 시 |
| [MODULE_MAP.md](MODULE_MAP.md) | 모든 소스 파일의 역할, 인터페이스, 의존성 맵 | 코드 수정 전 반드시 |
| [BUILD_AND_TEST.md](BUILD_AND_TEST.md) | 빌드, 실행, 테스트, 검증 방법 | 코드 수정 후 반드시 |
| [PROTOCOL.md](PROTOCOL.md) | OS ↔ 호스트 시리얼 통신 프로토콜 (개발/테스트 전용) | 시리얼 테스트 시 |
| [KNOWN_ISSUES.md](KNOWN_ISSUES.md) | 알려진 버그와 제약, 해결 상태 | 항상 |
| [Gemini.md](Gemini.md) | 프로젝트 기록(한국어). 개발 이력 참조용 | 컨텍스트 파악 시 |

---

## 2. LLM 교차 검증 개발 방법론 (Cross-Model Verification)

이 프로젝트는 **다양한 LLM 모델 간의 차이를 활용**하여 코드 최적화와 오류 최소화를 추구합니다.

### 핵심 원리
> LLM마다 강점과 약점이 다르다. A 모델이 놓치는 버그를 B 모델이 잡고, B 모델이 과도하게 복잡하게 만든 코드를 A 모델이 단순화한다.

### 워크플로우
```
┌──────────────────────────────────────────────────┐
│                  개발 사이클                        │
│                                                    │
│   [Model A: 개발]                                  │
│     └─ 코드 작성 + 자체 빌드 검증                      │
│            │                                       │
│            ▼                                       │
│   [Model B: 리뷰 & 검증]                            │
│     └─ 코드 리뷰, 버그 탐색, 성능 분석                  │
│     └─ 개선점 적용 또는 대안 제시                       │
│            │                                       │
│            ▼                                       │
│   [Model B: 개발]                                  │
│     └─ 다음 기능 구현                                │
│            │                                       │
│            ▼                                       │
│   [Model A: 리뷰 & 검증]                            │
│     └─ ...반복...                                  │
└──────────────────────────────────────────────────┘
```

### 각 모델이 코드를 인수받을 때 해야 하는 일
1. `HARNESS.md` (이 파일) 읽기 → 프로젝트 본질 이해
2. `MODULE_MAP.md` 읽기 → 현재 코드 구조 파악
3. `KNOWN_ISSUES.md` 읽기 → 알려진 문제 확인
4. 작업할 코드 파일 직접 읽기 → 전임 모델의 코드 이해
5. `cargo build` → 빌드 상태 확인
6. 작업 수행
7. `cargo build` → 수정 후 빌드 검증
8. 변경된 모듈의 `MODULE_MAP.md`, `KNOWN_ISSUES.md` 갱신

### 리뷰 체크리스트 (검증 모델용)
```markdown
- [ ] `no_std` 위반 없는가?
- [ ] 불필요한 `unwrap()` 사용은 없는가? (커널 패닉 위험)
- [ ] unsafe 블록에 충분한 안전성 근거가 있는가?
- [ ] AI 생성 코드가 샌드박스 밖에서 실행되지 않는가?
- [ ] 새 크레이트 추가 시 no_std 호환성이 검증되었는가?
- [ ] 성능에 불필요한 오버헤드를 추가하지 않았는가?
- [ ] 기존 인터페이스(함수 시그니처, 프로토콜 바이트)를 깨뜨리지 않았는가?
- [ ] cargo build가 에러 없이 통과하는가?
```

---

## 3. 절대 원칙 (Absolute Rules)

### 3.1 아키텍처
1. **키보드 + 모니터 + LAN은 OS가 자체적으로 동작시켜야 한다.** 외부 스크립트나 시리얼에 의존하지 않는 네이티브 구현이 최종 목표.
2. **샌드박스는 핵심 시스템을 제외한 모든 AI 생성 코드에 필수.** LLM 특성상 오류가 불가피하므로, Wasm 격리 실행으로 커널을 보호.
3. **성능 타협 불가.** 기존 OS 대비 성능이 떨어지면 안 됨. Rust 우선, 필요시 C/어셈블리 허용.
4. **외부 LLM은 항상 활용.** 로컬 LLM은 선택적(고성능 하드웨어 보유 시에만). 인터넷 연결이 가능하면 항상 외부 API 활용.
5. **OpenRhiza.com 생태계 연동.** 드라이버와 최적화 코드를 P2P로 공유하고, 신뢰도 기반으로 선별.

### 3.2 코드 레벨
6. **`#![no_std]` 엄수** — `std` 크레이트 사용 금지. `alloc`만 허용 (힙 할당자 초기화 후).
7. **`unsafe` 최소화** — MMIO, I/O 포트 접근 등 물리 하드웨어 접근에만 사용. 반드시 주석으로 *왜 unsafe가 필요한지* 설명.
8. **커널 패닉 금지** — `unwrap()` 대신 `if let` / `match` 사용. 예외 발생 시 로그로 전달.
9. **시리얼 통신은 개발/테스트 전용.** 프로덕션 동작에 시리얼 의존성을 두지 않는다.
10. **새 크레이트 추가 시 반드시 논의** — `no_std` 호환 여부 검증 필수.

### 3.3 프로세스 레벨
11. **빌드 검증 필수** — 코드 수정 후 `cargo build` 성공 확인 없이 커밋 금지.
12. **문서 동기화** — 모듈 추가/삭제/인터페이스 변경 시 `MODULE_MAP.md` 반드시 갱신.

---

## 4. 현재 달성 상태

- [x] 베어메탈 x86_64 QEMU 부팅
- [x] IDT 예외 처리 (Page Fault, GP Fault 방어)
- [x] PIC 8259 + 키보드 인터럽트(IRQ 1) 수신
- [x] 시리얼 포트(COM1) 양방향 통신 (테스트용)
- [x] VGA 텍스트 버퍼 (스크롤, 특수키)
- [x] PCI 버스 스캔 (Enumeration)
- [x] 힙 메모리 할당자 (linked_list_allocator, 1MB)
- [x] Wasm 샌드박스 (wasmi) — Host Functions 포함
- [x] 동적 키보드 드라이버 (256바이트 시리얼 주입) — **완전한 QWERTY 구현 필요**
- [x] smoltcp TCP/IP 스택 초기화
- [x] ATA PIO 읽기 + FAT16 파싱
- [ ] **완전한 QWERTY 키보드 네이티브 구현** ← 우선
- [ ] **네이티브 e1000 NIC 초기화 (OS 자체 LAN 확보)** ← 우선
- [ ] 실제 네트워크 패킷 송수신
- [ ] PIT/HPET 기반 실시간 타이머
- [ ] ATA PIO 쓰기 (Wasm 캐시 저장)
- [ ] 멀티 Wasm 인스턴스 동시 관리
- [ ] DNS + HTTP/HTTPS (외부 API 직접 접속)
- [ ] OpenRhiza.com 생태계 연동
- [ ] 로컬 LLM 탑재 (선택적, GPU/NPU/LPU 보유 시)

---

## 5. 하드웨어 커버리지 로드맵 (Hardware Coverage Roadmap)

> **필수:** 어떤 PC에서든 부팅되어 최소한의 I/O가 동작해야 합니다.  
> 현재는 QEMU 환경(PS/2 + VGA Text + e1000)으로 검증 중이며, 실물 하드웨어 범용성은 향후 반드시 추가 구현해야 합니다.

| 영역 | 현재 (QEMU 검증용) | 향후 필수 추가 | 이유 |
|------|---------------------|-----------------|------|
| **Input** | PS/2 키보드 ✅ | xHCI + USB HID Boot Protocol | USB 키보드가 없는 PC에서 키보드 불능 |
| **Display** | VGA Text Mode ✅ | Linear Framebuffer (비트맵 폰트) | UEFI-only 시스템에서 화면 불능 |
| **Network** | e1000 ✅ | Realtek RTL8111/8168 | 가성비 데스크탑 60%+ 지원 위해 |
| **Network** | - | WiFi (Intel/Broadcom) | 노트북 지원 — Layer 1+ (AI 생성) |

> ⚠️ **xHCI(USB 호스트 컨트롤러)**가 가장 크고 중요한 작업입니다.  
> xHCI가 있어야: USB 키보드, USB NIC 어댑터, USB 저장장치 모두 가능해집니다.

---

## 6. 우선순위 작업 큐

> **현재 목표:** QEMU에서 네트워크 통신 검증 → 외부 API 접속

| # | 작업 | 설명 | 상태 |
|---|------|------|------|
| 1 | ✅ 완전한 QWERTY 키보드 | PS/2 Scancode Set 1 네이티브 완전 구현 | 완료 |
| 2 | ✅ PIT 타이머 | ~1000Hz 실시간 ms 카운터 | 완료 |
| 3 | ✅ 네이티브 e1000 NIC | NIC 리셋, EEPROM MAC, RX/TX Ring, smoltcp 연동 | 완료 |
| 4 | **DHCP 클라이언트** | 자동 IP/Gateway/DNS 할당 (smoltcp 내장) | ⭐ 다음 |
| 5 | **DNS 클라이언트** | 도메인 → IP 변환 | 대기 (#4 의존) |
| 6 | **HTTP 클라이언트** | TCP 소켓으로 OpenRhiza.com / LLM API 접속 | 대기 (#5 의존) |
| 7 | TLS 1.3 | HTTPS 보안 통신 | 대기 (#6 의존) |
| 8 | ATA PIO 쓰기 | Wasm 드라이버 캐시 영속화 | 대기 |
| 9 | xHCI + USB HID | 실물 하드웨어 키보드 지원 | 대기 |
| 10 | Framebuffer 텍스트 렌더러 | UEFI 시스템 화면 출력 | 대기 |
| 11 | RTL8168 NIC | 데스크탑 네트워크 범용성 | 대기 |

---

## 6. 시리얼 통신의 위치 (개발용 vs 프로덕션)

> **시리얼(COM1 UART)은 개발/테스트 도구입니다. 프로덕션 핵심 인프라가 아닙니다.**

- 현재: QEMU 환경에서 `host_brain.py`와 통신하는 디버깅 파이프라인으로 유용
- 프로덕션: 키보드(네이티브) + 모니터(VGA/Framebuffer) + LAN(네이티브 e1000)이 핵심
- `println!` 매크로는 화면 출력이 주 목적. 시리얼 출력은 디버그 모드에서만 활성화하도록 향후 분리
